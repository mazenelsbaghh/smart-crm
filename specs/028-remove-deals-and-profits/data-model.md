# Data Model: remove-deals-and-profits

**Created**: 2026-06-23

No database schema changes are introduced. The database models (such as `Customer` and `Deal`) and tables are kept intact for system stability, but the fields `budget` and `pipelineStage` are no longer exposed or editable in the user interface.
